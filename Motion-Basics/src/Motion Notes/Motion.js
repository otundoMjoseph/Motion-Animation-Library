/*
Motion is an animation Library For React.

When we use Motion. we are indicating that we want to use that element.
Motion then provied specific props to animate the element such as initial, animate, exit, whilHover,
allowing fror more powerful and customizable animations.

Initial Prop is use to set the initial state of the element before it enters the DOM.
Animate prop is used  to create animations for a component. It lets you set target values for different
CSS properties making it easy to have smooth changes when the components state updates.
Exit prop is used to define the animation that occurs when a component is removed from the react tree.
This is particularly useful when creating smooth transitions when components are unmounted/removed.
enhancing the user experience indcating that something has been removed or disappeared.

Tranformations: they allow you to change the shape the size of the component.

Transition: Theys are effects that contorl how animations happen. This are rules for an element moves.
Transition Properties: duration, ease, delay.
Duration: The time it takes for an animation to complete.
Delay: The time before an animation starts.
Ease: The speed curve of an animation. It defines how the animation progresses over time.
 It can be linear, ease-in, ease-out, or custom cubic-bezier curves.


 Keyframes: They allow us to create more complex animations by specifying multiple points in 
 an animation sequence.
Instead of just animatiing from one state to another, we can define several intermediate states(frames)
to contorl the animation more precisely.
[start, end]

*/